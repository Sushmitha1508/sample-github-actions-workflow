name : multi event pipeline / workflow
on: 
    push:
        branches:
            - main
            - test
    pull_request:
        branches:
            - main
            - test 
    workflow_dispatch: # this will allow us to trigger the workflow manually from the github actions tab in the repository             

jobs:
    on-push-job:
        if: github.event_name == 'push'
        runs-on: ubuntu-latest
        steps:
            - name: "on push event"
              run: echo "this is on push event"
    on-pull-request-job:
        if: github.event_name == 'pull_request'
        runs-on: ubuntu-latest
        steps:
            - name: "on pull request event"
              run: echo "this is on pull request event"           